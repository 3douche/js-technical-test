var React = require('react');

var App = React.createClass({

    render: function () {

        var toto = "TOTO";

        return (
            <div>{toto}</div>
        );
    }

});

module.exports = App;